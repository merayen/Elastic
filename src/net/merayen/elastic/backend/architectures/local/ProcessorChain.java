package net.merayen.elastic.backend.architectures.local;

import java.util.List;

/**
 * A chain is a collection of connected nodes that share the same poly_no.
 */
public class ProcessorChain {
	List<LocalProcessor> processors = new ArrayList<>();
	//public Chain(LocalProcessor[] )
}
