package net.merayen.elastic.backend.storage.resource;

/**
 * Not sure, perhaps be managing the revisions. Perhaps wrongly placed.
 */
public class RevisionManager {

	private final ResourceManager resource_manager;

	public RevisionManager(ResourceManager rm) {
		resource_manager = rm;
	}
}
